function syncUrlToWindow(iframeWindow) {
    const {sync, id, prefix} = iframeWindow.__mf;
    let winUrlElement = anchor;
    const queryMap = geetArch;

    if (!sync && !queryMap[id]) {
        return (winUrlElement = null);
    }
}
